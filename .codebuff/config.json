{
  "mcpServers": {
    "zapier": {
      "command": "npx",
      "args": [
        "-y",
        "@zapier/zapier-mcp"
      ],
      "env": {
        "ZAPIER_MCP_TOKEN": "${ZAPIER_MCP_TOKEN}"
      }
    }
  }
}
