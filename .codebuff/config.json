{
  "mcpServers": {
    "zapier": {
      "command": "npx",
      "args": [
        "-y",
        "@zapier/zapier-mcp"
      ],
      "env": {
        "ZAPIER_MCP_TOKEN": "${ZAPIER_MCP_TOKEN}"
      },
      "_comment": "IMPORTANTE: El token debe establecerse como variable de entorno ZAPIER_MCP_TOKEN. NO guardar tokens reales en este archivo."
    }
  }
}
