"""MASSIVE — top-level package.

Single source of truth for the project version.  Consumed by
``pyproject.toml`` (dynamic version), the FastAPI app, the CLI and
frontend ``package.json`` (see W3-T04 versionado único).
"""

__version__ = "1.0.0"
